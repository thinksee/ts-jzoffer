package com.thinksee.jzoffer;

/**
 * 输入一个递增排序的数组和一个数字s，
 * 在数组中查找两个数，使得它们和正好为s。
 * 若存在多对数字和为s，输出任意一对即可。
 */
public class J41NumbersSum {
    public static int[] getNumbersWithSum(int[] nums, int sum){
        return new int[]{-1, -1};
    }
}
